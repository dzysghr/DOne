<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.dzy.done"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 获取移动网络信息 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- 获取WIFI信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:name=".config.app"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".view.activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.ArticleActiviry">
        </activity>
        <activity android:name=".view.activity.PictureActivity">
        </activity>
        <activity android:name=".view.activity.ThingActivity">
        </activity>
        <activity android:name=".view.activity.PhotoViewerActivity">
        </activity>
        <activity android:name=".TestActivity">

        </activity>
    </application>

</manifest>
